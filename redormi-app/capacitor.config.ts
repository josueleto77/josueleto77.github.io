import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.redormi.app",
  appName: "Redormi",
  webDir: "capacitor-www",
  backgroundColor: "#F7F4EE",
  android: {
    backgroundColor: "#F7F4EE",
  },
  ios: {
    backgroundColor: "#F7F4EE",
  },
};

export default config;
